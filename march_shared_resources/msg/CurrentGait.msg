Header header
# Gait name that is executing
string gait
# Subgait name that is executing, if the gait has subgaits, else empty string
string subgait
# Optional version name of the gait/subgait that is executing
string version
# Optional gait type name of the gait/subgait that is executing
string gait_type
